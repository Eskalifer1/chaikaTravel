/** URL search param key used to persist the room query */
export const ROOM_QUERY_PARAM = "q";

/** Debounce delay in ms for URL updates while the user is typing */
export const SEARCH_DEBOUNCE_MS = 400;

/** URL param key for check-in date (ISO YYYY-MM-DD) */
export const CHECK_IN_PARAM = "checkIn";

/** URL param key for check-out date (ISO YYYY-MM-DD) */
export const CHECK_OUT_PARAM = "checkOut";

/** URL param key for number of rooms */
export const ROOMS_PARAM = "rooms";

/** URL param key for number of adults */
export const ADULTS_PARAM = "adults";

/** URL param key for comma-separated child ages */
export const CHILD_AGES_PARAM = "childAges";
